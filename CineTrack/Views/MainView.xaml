<UserControl x:Class="CineTrack.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CineTrack.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="#1a1a2e"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0"
                Background="#16213e">
            <Grid Margin="20,0">
                <TextBlock Text="CineTrack"
                           Foreground="#e94560"
                           FontSize="22"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <!-- Barre de recherche -->
<<<<<<< HEAD
                <Border Width="250"
                        Height="32"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Background="#0f3460"
                        CornerRadius="8" 
                        BorderBrush="#1a3a6e"
                        BorderThickness="1">
                    <TextBox Background="Transparent"
                             Foreground="#888"
                             CaretBrush="White"
                             BorderThickness="0"
                             Padding="10,0"
                             VerticalContentAlignment="Center"
                             Text="Rechercher un anime..."/>
                </Border>
=======
                <TextBox Width="250"
                         Height="32"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Center"
                         Background="#0f3460"
                         Foreground="#888"
                         CaretBrush="White"
                         BorderBrush="#0f3460"
                         Padding="10,0"
                         Text="(FAKE BAR) Rechercher un anime..."/>
>>>>>>> 03e09d13d612898c4b856948578a27aac2681c62

                <!-- Bouton favoris -->
                <Button Content="Mes favoris"
                        Width="110"
                        Height="34"
                        HorizontalAlignment="Right"
                        Margin="0,0,125,0"
                        Background="#e94560"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding FavorisCommand}"/>

                <!-- Bouton disconnect -->
                <Button Content="Se déconnecter"
                        Width="110"
                        Height="34"
                        HorizontalAlignment="Right"
                        Background="#e94560"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding DisconnectCommand}"/>
            </Grid>
        </Border>

<<<<<<< HEAD
        <!-- MAIN CONTENT -->
        <ScrollViewer x:Name="LandingPage"
                      Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,30">
=======
        <!-- LOADING -->
        <TextBlock Grid.Row="1"
                   Text="Chargement..."
                   Foreground="#888"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>

        <!-- CAROUSELS -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisInverseConverter}}">
            <StackPanel Margin="0,30">
>>>>>>> 03e09d13d612898c4b856948578a27aac2681c62

                <TextBlock Text="Découvrez les meilleurs animes"
                           Foreground="White"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Suivez, notez et partagez vos animes préférés"
                           Foreground="#888"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,40"/>

                <ItemsControl ItemsSource="{Binding Carousels}"
                              Margin="20,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,30">

<<<<<<< HEAD
                    <!-- Anime 1 -->
                    <StackPanel Margin="10">
                        <Border Width="180" Height="260"
                                Background="#e94560"
                                CornerRadius="8"/>
                        <TextBlock Text="Anime 1"
                                   Foreground="White"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,2"/>
                        <TextBlock Text="Description courte"
                                   Foreground="#888"
                                   FontSize="12"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Anime 2 -->
                    <StackPanel Margin="10">
                        <Border Width="180" Height="260"
                                Background="#0f3460"
                                CornerRadius="8"/>
                        <TextBlock Text="Anime 2"
                                   Foreground="White"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,2"/>
                        <TextBlock Text="Description courte"
                                   Foreground="#888"
                                   FontSize="12"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Anime 3 -->
                    <StackPanel Margin="10">
                        <Border Width="180" Height="260"
                                Background="#16213e"
                                CornerRadius="8"/>
                        <TextBlock Text="Anime 3"
                                   Foreground="White"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,2"/>
                        <TextBlock Text="Description courte"
                                   Foreground="#888"
                                   FontSize="12"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                </StackPanel>
=======
                                <!-- Carousel title -->
                                <StackPanel Orientation="Horizontal"
                                            Margin="0,0,0,12">
                                    <TextBlock Text="{Binding GenreName}"
                                               Foreground="White"
                                               FontSize="20"
                                               FontWeight="Bold"/>
                                    <TextBlock Text=" — chargement..."
                                               Foreground="#888"
                                               FontSize="14"
                                               VerticalAlignment="Bottom"
                                               Margin="8,0,0,2"
                                               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>
                                </StackPanel>

                                <!-- Carousel row: prev button + cards + next button -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Previous button -->
                                    <Button Grid.Column="0"
                                            Content="❮"
                                            Command="{Binding PreviousCommand}"
                                            Width="32"
                                            Height="32"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Background="#0f3460"
                                            Foreground="White"
                                            BorderThickness="0"
                                            FontSize="14"
                                            Cursor="Hand"/>

                                    <!-- Anime cards — fixed size, clipped so they never squish -->
                                    <ScrollViewer Grid.Column="1"
                                                  HorizontalScrollBarVisibility="Hidden"
                                                  VerticalScrollBarVisibility="Disabled"
                                                  IsHitTestVisible="False">
                                        <ItemsControl ItemsSource="{Binding VisibleAnimes}"
                                                      SizeChanged="CardsPanel_SizeChanged">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="150"
                                                            Height="220"
                                                            Margin="6,0"
                                                            CornerRadius="8"
                                                            ClipToBounds="True"
                                                            Background="#16213e"
                                                            Cursor="Hand">
                                                        <Grid>
                                                            <Image Source="{Binding ImageUrl}"
                                                                   Stretch="UniformToFill"/>
                                                            <Border VerticalAlignment="Bottom"
                                                                    CornerRadius="0,0,8,8">
                                                                <Border.Background>
                                                                    <LinearGradientBrush StartPoint="0,0"
                                                                                         EndPoint="0,1">
                                                                        <GradientStop Color="Transparent"
                                                                                      Offset="0"/>
                                                                        <GradientStop Color="#EE000000"
                                                                                      Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Border.Background>
                                                                <StackPanel Margin="8,4">
                                                                    <TextBlock Text="{Binding Title}"
                                                                               Foreground="White"
                                                                               FontSize="11"
                                                                               FontWeight="SemiBold"
                                                                               TextWrapping="Wrap"
                                                                               MaxHeight="36"/>
                                                                    <TextBlock Text="{Binding CommunityScoreDisplay}"
                                                                               Foreground="#f0c040"
                                                                               FontSize="10"
                                                                               Margin="0,2,0,4"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>

                                    <!-- Next button -->
                                    <Button Grid.Column="2"
                                            Content="❯"
                                            Command="{Binding NextCommand}"
                                            Width="32"
                                            Height="32"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Background="#0f3460"
                                            Foreground="White"
                                            BorderThickness="0"
                                            FontSize="14"
                                            Cursor="Hand"/>
                                </Grid>

                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

>>>>>>> 03e09d13d612898c4b856948578a27aac2681c62
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>